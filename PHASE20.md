# Phase 20 — Frontend Integration

Adds a Next.js frontend consuming real Phase 19 backend endpoints.

## Scope
- Public passenger dashboard
- Live bus/route/alert data
- Journey planner
- Admin login and dashboard overview
- Shared API client with JWT storage

No transport mock dataset is bundled. OSM map rendering remains an explicit integration point because a production tile/routing provider was not specified.
