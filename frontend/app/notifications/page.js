'use client';
import{useEffect,useState}from'react';import{api}from'../../lib/api';
export default function Notifications(){const[data,setData]=useState([]),[err,setErr]=useState(''),[loading,setLoading]=useState(true);useEffect(()=>{if(!localStorage.getItem('passenger_token')){setLoading(false);return}api('/api/v1/passenger/notifications').then(x=>setData(x||[])).catch(e=>setErr(e.message)).finally(()=>setLoading(false))},[]);return <main className="container"><div className="title"><div><span className="eyebrow">Passenger</span><h1>Notifications</h1></div><a className="btn secondary" href="/account">Preferences</a></div>{!localStorage.getItem('passenger_token')?<div className="card"><p>Please sign in to view your notifications.</p><a className="btn" href="/account">Sign in</a></div>:loading?<div className="card">Loading…</div>:err?<p className="error">{err}</p>:<div className="list">{data.length?data.map(n=><article className="card" key={n.id}><div className="title"><b>{n.title||n.type||'Notification'}</b><span className="badge">{n.channel||'In app'}</span></div><p>{n.message||n.body||''}</p><span className="small muted">{n.createdAt?new Date(n.createdAt).toLocaleString():''}</span></article>):<p className="muted">No notifications.</p>}</div>}</main>}
