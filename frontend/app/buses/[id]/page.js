'use client';import{useEffect,useState}from'react';import{api}from'../../../lib/api';
export default function Bus({params}){const[id,setId]=useState(null),[bus,setBus]=useState(null),[occ,setOcc]=useState(null),[eta,setEta]=useState(null),[err,setErr]=useState('');useEffect(()=>{Promise.resolve(params).then(p=>setId(p.id))},[params]);useEffect(()=>{if(!id)return;Promise.all([api(`/api/v1/public/buses`),api(`/api/v1/public/buses/${id}/occupancy`),api(`/api/v1/eta/buses/${id}`)]).then(([bs,o,e])=>{setBus(bs.find(x=>x.id===id));setOcc(o);setEta(e)}).catch(e=>setErr(e.message))},[id]);if(err)return <main className="container"><p className="error">{err}</p></main>;if(!bus)return <main className="container"><p>Loading…</p></main>;return <main className="container"><h1>Bus {bus.busNumber}</h1><div className="grid"><div className="card"><b>Status</b><p>{bus.status}</p><p>Line {bus.lineNumber||'—'} · {bus.direction||'—'}</p></div><div className="card"><b>Occupancy</b><p>{bus.passengerCount??occ?.passengerCount??'—'} / {bus.capacity}</p><p className={bus.full?'full':''}>{bus.full?'FULL':'Available'}</p></div><div className="card"><b>Live position</b><p>{bus.latitude??'—'}, {bus.longitude??'—'}</p><p>{bus.speedKph??'—'} km/h</p></div><div className="card"><b>ETA</b><pre>{JSON.stringify(eta,null,2)}</pre></div></div><div className="map">OSM live position integration point</div></main>}
